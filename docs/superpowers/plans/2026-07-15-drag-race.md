# Драг-рейсинг «Голубиные гонки» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Отдельный интерактивный драг-заезд голубей (реакция на старте + мощность тюнинга решают исход), два режима (Тренировка/Ставка), призраки из реальных голубей по мощности. Спека `docs/superpowers/specs/2026-07-15-drag-race-design.md`.

**Architecture:** Новый серверный модуль `src/drag.ts` (чистые функции физики заезда + `pickOpponents` matchmaking + `runRace` резолв в транзакции). HTTP — в существующий `src/routes/pigeons.ts` (2 роута). Схема — 1 колонка на `clicker_state`. Клиент — новый `public/js/catdrag.js` (canvas сайд-скролл заезд), вход из секции гонки в `catdove.js`. Недельная гонка/дивизионы НЕ трогаются.

**Tech Stack:** Node 20 + TypeScript + Express + node-postgres; клиент — vanilla JS + Canvas 2D. Тесты: vitest (`npm test`, есть `tests/`).

## Global Constraints

- Ключ игрока `chat_id BIGINT` (VK-сдвиг 2e12, платформа-агностично).
- Транзакции ТОЛЬКО `pool.connect()` + BEGIN/COMMIT/ROLLBACK + `client.release()` в `finally`; каждый ранний выход после BEGIN → ROLLBACK. НИКОГДА `pool.query("BEGIN")`.
- Схема через `ALTER ... ADD COLUMN IF NOT EXISTS` в `initPigeonSchema` (совместимо с живой БД).
- Мощность голубя берётся ТОЛЬКО из БД (`pigeon_inventory.tune_*` + порода). Клиент шлёт лишь `reactionMs` — зажимать в `[120, 3000]`.
- Списание энергии/ставки и резолв — в ОДНОЙ транзакции (нет «ретрая до победы»).
- Импорты из `clicker.ts` внутри `drag.ts`/`pigeons.ts` — только ленивые (`await import`), как в существующем коде (цикл clicker↔pigeons).
- Все новые роуты: `requireTgUser` + `rateLimit`. `chatId = getTgUser(req)!.id`.
- RU строки UI; EN идентификаторы. После каждой задачи `npx tsc --noEmit` + `npx vitest run` зелёные → commit. Не пушить до финала.
- Энергия: поле `clicker_state.energy INT`, реген применяется в `refresh()` (clicker.ts). Для драга энергию читать ПОСЛЕ регена (через refresh или пересчёт), списывать атомарно.

---

### Task 1: Физика заезда — чистые функции (`src/drag.ts`) + vitest

**Files:**
- Create: `src/drag.ts`
- Create/Modify: `tests/drag.test.ts`

**Interfaces:**
- Produces: `DRAG_ENERGY_COST`, `TRACK_LEN`, `BASE_SPEED`, `SPEED_PER_POWER`, `REACT_WEIGHT`, `LUCK_SPREAD`, `POWER_BAND`, `STAKE_PRESETS`, `PAYOUT` (карта место→множитель),
  `dragPower(rarity, stars, speed, stamina): number`,
  `dragFinishTime(power, reactionMs, r): number`,
  `resolveRace(racers: {power:number, reactionMs:number, r:number}[]): number[]` — массив мест (index→place, 1=победа).
- Consumes: тип `Rarity` из `./pigeons` (импорт типа).

- [ ] **Step 1: Написать `tests/drag.test.ts` (RED)** — сначала отношения, не магические числа:

```ts
import { describe, it, expect } from "vitest";
import { dragPower, dragFinishTime, resolveRace, DRAG_ENERGY_COST } from "../src/drag";

describe("dragPower — мощность голубя для заезда", () => {
  it("растёт со скоростью/выносливостью и редкостью, детерминированна", () => {
    expect(dragPower("legendary", 3, 10, 10, 0)).toBeGreaterThan(dragPower("common", 1, 0, 0, 0));
    expect(dragPower("common", 1, 5, 0, 0)).toBeGreaterThan(dragPower("common", 1, 0, 0, 0));
    expect(dragPower("common", 1, 0, 0, 0)).toBe(dragPower("common", 1, 0, 0, 0)); // без рандома
  });
});

describe("dragFinishTime — финишное время (меньше = быстрее)", () => {
  it("мощнее голубь финиширует раньше (при равной реакции/рандоме)", () => {
    const strong = dragFinishTime(150, 300, 0.5);
    const weak = dragFinishTime(20, 300, 0.5);
    expect(strong).toBeLessThan(weak);
  });
  it("быстрее реакция → раньше финиш (при равной мощности)", () => {
    expect(dragFinishTime(80, 150, 0.5)).toBeLessThan(dragFinishTime(80, 900, 0.5));
  });
  it("реакция зажимается: <120мс не даёт преимущества сверх 120", () => {
    expect(dragFinishTime(80, 0, 0.5)).toBe(dragFinishTime(80, 120, 0.5));
    expect(dragFinishTime(80, 5000, 0.5)).toBe(dragFinishTime(80, 3000, 0.5));
  });
});

describe("resolveRace — места по возрастанию finishT + доминирование мощности", () => {
  it("МОЩНОСТЬ ГЛАВНЕЕ: сильный голубь с плохой реакцией обходит слабого с идеальной", () => {
    // сильный (power 150, реакция 900) vs слабый (power 20, реакция 120)
    const places = resolveRace([{ power: 150, reactionMs: 900, r: 0.5 }, { power: 20, reactionMs: 120, r: 0.5 }]);
    expect(places[0]).toBe(1); // сильный выиграл несмотря на худшую реакцию
    expect(places[1]).toBe(2);
  });
  it("при РАВНОЙ мощности решает реакция", () => {
    const places = resolveRace([{ power: 80, reactionMs: 700, r: 0.5 }, { power: 80, reactionMs: 200, r: 0.5 }]);
    expect(places[1]).toBe(1); // у кого реакция лучше — тот первый
  });
  it("места уникальны и покрывают 1..N", () => {
    const places = resolveRace([{ power: 100, reactionMs: 300, r: 0.1 }, { power: 90, reactionMs: 300, r: 0.5 }, { power: 110, reactionMs: 300, r: 0.9 }]);
    expect([...places].sort()).toEqual([1, 2, 3]);
  });
});
```

- [ ] **Step 2: Прогнать (RED)** — `npx vitest run tests/drag.test.ts` → падает (нет `src/drag.ts`).

- [ ] **Step 3: Написать `src/drag.ts` (чистая часть)** — подобрать константы так, чтобы тесты «мощность главнее» и «равная мощность → реакция» проходили ОДНОВРЕМЕННО:

```ts
// src/drag.ts — драг-рейсинг: физика заезда (чистые функции) + подбор соперников + резолв.
// Спека: docs/superpowers/specs/2026-07-15-drag-race-design.md
import type { Rarity } from "./pigeons";

export const DRAG_ENERGY_COST = 250;
export const TRACK_LEN = 2000;
export const BASE_SPEED = 220;
export const SPEED_PER_POWER = 5;      // мощность доминирует: разрыв power перевешивает реакцию
export const REACT_MIN = 120, REACT_MAX = 3000;
export const REACT_WEIGHT = 0.4;       // сек за секунду задержки — мал относительно разрыва мощности
export const LUCK_SPREAD = 0.15;       // маленький рандом (сек)
export const POWER_BAND = 25;          // коридор подбора соперников по мощности
export const STAKE_PRESETS = [500, 2000, 10000];
export const PAYOUT: Record<number, number> = { 1: 2, 2: 1, 3: 0, 4: 0 }; // множитель к ставке (2=+ставка, 1=возврат, 0=потеря)

const RARITY_BASE: Record<Rarity, number> = { common: 10, rare: 16, epic: 22, legendary: 28 };

export function dragPower(rarity: Rarity, stars: number, speed: number, stamina: number): number {
  return RARITY_BASE[rarity] + (stars - 1) * 4 + 6 * speed + 6 * stamina;
}

const clampReact = (ms: number) => Math.min(REACT_MAX, Math.max(REACT_MIN, ms));

export function dragFinishTime(power: number, reactionMs: number, r: number): number {
  const speed = BASE_SPEED + power * SPEED_PER_POWER;
  const reactDelay = (clampReact(reactionMs) / 1000) * REACT_WEIGHT;
  return TRACK_LEN / speed + reactDelay + r * LUCK_SPREAD;
}

// Места по возрастанию finishT (1 = победа). Тай-брейк по индексу (стабильно).
export function resolveRace(racers: { power: number; reactionMs: number; r: number }[]): number[] {
  const times = racers.map((x, i) => ({ i, t: dragFinishTime(x.power, x.reactionMs, x.r) }));
  times.sort((a, b) => a.t - b.t || a.i - b.i);
  const places = new Array(racers.length);
  times.forEach((x, rank) => { places[x.i] = rank + 1; });
  return places;
}
```
Проверка калибровки: сильный (power 150) финиш ≈ 2000/(220+750)=2.06 + реакция; слабый (power 20) ≈ 2000/320=6.25. Разрыв 4с >> реакция (макс 0.4×3=1.2с) → мощность доминирует ✓. При равной мощности разрыв времён = только реакция+luck ✓. Если тест не проходит — крутить `SPEED_PER_POWER`/`REACT_WEIGHT`, НЕ тесты.

- [ ] **Step 4: GREEN** — `npx vitest run tests/drag.test.ts` зелёный. Затем `npx tsc --noEmit`.

- [ ] **Step 5: Commit** — `git add src/drag.ts tests/drag.test.ts && git commit -m "feat(drag): физика заезда — dragPower/dragFinishTime/resolveRace + vitest"`

---

### Task 2: Схема + подбор соперников (`pickOpponents`)

**Files:**
- Modify: `src/pigeons.ts` (ALTER в `initPigeonSchema`)
- Modify: `src/drag.ts` (добавить `pickOpponents`)

**Interfaces:**
- Consumes: `pool` (импорт как в pigeons.ts), `PIGEON_BREEDS`/`BREED_BY_ID` из `./pigeons`, `dragPower`, `POWER_BAND`.
- Produces: `type Racer = { breed: string; power: number; reactionMs: number; bot: boolean; name?: string }`;
  `pickOpponents(chatId: number, targetPower: number, n: number): Promise<Racer[]>` — n соперников (без самого игрока), реальные по мощности + добивка ботами.

- [ ] **Step 1: ALTER колонки в `initPigeonSchema`** (`src/pigeons.ts`, рядом с `race_reaction_ms`... его ещё нет — добавить):
```ts
await pool.query(`ALTER TABLE clicker_state ADD COLUMN IF NOT EXISTS race_reaction_ms SMALLINT`);
```

- [ ] **Step 2: `pickOpponents` в `src/drag.ts`:**
```ts
import { pool } from "./db"; // сверить точное имя экспорта пула (как в pigeons.ts) и повторить
import { PIGEON_BREEDS, BREED_BY_ID } from "./pigeons";

export type Racer = { breed: string; power: number; reactionMs: number; bot: boolean; name?: string };

export async function pickOpponents(chatId: number, targetPower: number, n: number): Promise<Racer[]> {
  // реальные голуби других игроков в коридоре мощности ±POWER_BAND, ближайшие по |power-target|
  const rows = (await pool.query(
    `SELECT pi.breed, pi.stars, pi.tune_speed, pi.tune_stamina, cs.race_reaction_ms
       FROM pigeon_inventory pi JOIN clicker_state cs ON cs.chat_id = pi.chat_id
      WHERE pi.chat_id <> $1 AND pi.count > 0`, [chatId])).rows;
  const real: Racer[] = rows.map((r: any) => {
    const b = BREED_BY_ID.get(r.breed)!;
    const power = /* dragPower */ 0; // импортировать dragPower и посчитать: dragPower(b.rarity, r.stars, r.tune_speed, r.tune_stamina)
    return { breed: r.breed, power, reactionMs: r.race_reaction_ms ?? synthReaction(targetPower), bot: false };
  }).filter(x => Math.abs(x.power - targetPower) <= POWER_BAND)
    .sort((a, b) => Math.abs(a.power - targetPower) - Math.abs(b.power - targetPower))
    .slice(0, n);
  // добивка синтетикой под target
  while (real.length < n) real.push(makeBot(targetPower, real.length));
  return real;
}
```
Дописать хелперы: `dragPower` импортировать; `synthReaction(target)` — правдоподобная реакция (напр. `250 + Math.round((target%7)*40)` — детерминированно без Math.random в чистом хелпере, либо принять r-параметр); `makeBot(target, seed)` — случайная порода под редкость + мощность ≈ target (подобрать tune так, чтобы dragPower≈target), `bot:true`, `name:"Соперник"`. Math.random допустим в makeBot (не чистая функция, серверная). Порода бота — любая из PIGEON_BREEDS кроме champion.

- [ ] **Step 3: Проверить** — `npx tsc --noEmit` чисто. (Живой БД нет — корректность SQL чтением; чистая логика dragPower уже под тестом.)

- [ ] **Step 4: Commit** — `git add src/drag.ts src/pigeons.ts && git commit -m "feat(drag): колонка race_reaction_ms + подбор соперников по мощности (+синтетика)"`

---

### Task 3: Резолв заезда в транзакции (`runRace`)

**Files:**
- Modify: `src/drag.ts`

**Interfaces:**
- Consumes: `pickOpponents`, `resolveRace`, `dragPower`, `DRAG_ENERGY_COST`, `PAYOUT`, `STAKE_PRESETS`; ленивый `await import("./clicker")` для энергии/баланса при необходимости.
- Produces: `runRace(chatId, breed, mode: "training"|"bet", stake: number, reactionMs: number): Promise<{ ok, racers?, myPlace?, reward?, newBalance?, newEnergy?, reason? }>`.
  `racers` — массив `{ breed, power, finishT, me, bot }` (finishT для анимации на клиенте), отсортированный по месту.

- [ ] **Step 1: Реализация `runRace`** (транзакция: FOR UPDATE clicker_state, реген энергии, проверки, списания, резолв, выплата):
```ts
export async function runRace(chatId: number, breed: string, mode: "training" | "bet", stake: number, reactionMs: number):
  Promise<{ ok: boolean; racers?: any[]; myPlace?: number; reward?: number; newBalance?: number; newEnergy?: number; reason?: string }> {
  if (!BREED_BY_ID.has(breed)) return { ok: false, reason: "not_owned" };
  if (mode !== "training" && mode !== "bet") return { ok: false, reason: "bad_mode" };
  if (mode === "bet" && !STAKE_PRESETS.includes(stake)) return { ok: false, reason: "bad_stake" };
  const client = await pool.connect();
  try {
    await client.query("BEGIN");
    // энергия с регеном: проще всего дернуть refresh (clicker) в этой же транзакции, чтобы energy был актуальным
    const { refreshEnergyFor } = await import("./clicker"); // ЭКСПОРТ добавить в clicker.ts (см. ниже) — применяет реген и возвращает {energy, balance, row}
    const st = await refreshEnergyFor(client, chatId); // {energy, balance}
    // мой голубь
    const inv = await client.query(`SELECT stars, tune_speed, tune_stamina FROM pigeon_inventory WHERE chat_id=$1 AND breed=$2 AND count>0`, [chatId, breed]);
    if (!inv.rowCount) { await client.query("ROLLBACK"); return { ok: false, reason: "not_owned" }; }
    if (st.energy < DRAG_ENERGY_COST) { await client.query("ROLLBACK"); return { ok: false, reason: "no_energy" }; }
    if (mode === "bet" && st.balance < stake) { await client.query("ROLLBACK"); return { ok: false, reason: "not_enough_coins" }; }
    const b = BREED_BY_ID.get(breed)!;
    const myPower = dragPower(b.rarity, inv.rows[0].stars, inv.rows[0].tune_speed, inv.rows[0].tune_stamina);
    const opps = await pickOpponents(chatId, myPower, 3);
    // собрать всех гонщиков, посчитать места
    const react = Math.min(3000, Math.max(120, Math.round(reactionMs)));
    const field = [{ breed, power: myPower, reactionMs: react, bot: false, me: true },
                   ...opps.map(o => ({ ...o, me: false }))];
    const rs = field.map(f => ({ power: f.power, reactionMs: f.reactionMs, r: Math.random() }));
    const places = resolveRace(rs);
    const racers = field.map((f, i) => ({ breed: f.breed, power: f.power, finishT: /* dragFinishTime(rs[i]) */ 0, place: places[i], me: f.me, bot: f.bot }))
                        .sort((a, b) => a.place - b.place);
    const myPlace = places[0];
    // списания + выплата
    let energyLeft = st.energy - DRAG_ENERGY_COST, balance = st.balance, reward = 0;
    if (mode === "bet") {
      const mult = PAYOUT[myPlace] ?? 0;   // 2=+ставка, 1=возврат, 0=потеря
      reward = stake * mult - stake;       // net: +stake / 0 / -stake
      balance += reward;
    }
    await client.query(`UPDATE clicker_state SET energy=$2, balance=$3, race_reaction_ms=$4 WHERE chat_id=$1`,
      [chatId, energyLeft, balance, react]);
    await client.query("COMMIT");
    return { ok: true, racers, myPlace, reward, newBalance: balance, newEnergy: energyLeft };
  } catch (e) { await client.query("ROLLBACK"); throw e; }
  finally { client.release(); }
}
```
Уточнения для реализатора:
- `finishT` считать через `dragFinishTime(power, reactionMs, r)` тем же `r`, что в `rs` (сохранить r по индексу до сортировки).
- `refreshEnergyFor(client, chatId)` — НОВЫЙ экспорт в `clicker.ts`: применяет реген энергии к строке `clicker_state` (как начало `refresh()`: `energy = min(max, energy + secs*REGEN_PER_SEC)`, `updated_at=NOW()`) под `FOR UPDATE`, возвращает `{energy, balance}`. Вынести общую часть из `refresh()` или сделать тонкую функцию. НЕ дублировать формулу регена — переиспользовать существующие `energyMaxFor`/`REGEN_PER_SEC`.
- Баланс не уходит в минус: проверка `balance>=stake` до списания; при потере `balance += (-stake)` уже покрыта проверкой.

- [ ] **Step 2: Проверить** — `npx tsc --noEmit` + `npx vitest run` зелёные.

- [ ] **Step 3: Commit** — `git add src/drag.ts src/clicker.ts && git commit -m "feat(drag): резолв заезда в транзакции (энергия/ставка/выплата, античит-зажим реакции)"`

---

### Task 4: HTTP-роуты драга

**Files:**
- Modify: `src/routes/pigeons.ts`

**Interfaces:**
- Consumes: `runRace`, `pickOpponents`, `dragPower`, `STAKE_PRESETS` из `../drag`; `BREED_BY_ID` из `../pigeons`.
- Produces: `POST /api/pigeons/drag/opponents`, `POST /api/pigeons/drag/race`.

- [ ] **Step 1: Роуты** (по образцу существующих в файле — `getTgUser(req)!`, try/catch→500, 400 `{error}`):
```ts
router.post("/api/pigeons/drag/opponents", requireTgUser, rateLimit(30), async (req, res) => {
  const u = getTgUser(req)!; const breed = String((req.body as any).breed || "");
  try {
    const b = BREED_BY_ID.get(breed); if (!b) { res.status(400).json({ error: "not_owned" }); return; }
    // мощность моего голубя — из БД (владение проверит pickOpponents косвенно; тут просто превью)
    const { dragPower, pickOpponents } = await import("../drag");
    // прочитать мой тюнинг для target (или отдать pickOpponents вычислять) — сверить getTuning
    // ... собрать targetPower, вернуть pickOpponents(u.id, targetPower, 3)
    res.json({ opponents: await pickOpponents(u.id, /* targetPower */ 0, 3) });
  } catch (e) { log.error({ err: e, chatId: u.id }, "[drag/opponents]"); res.status(500).json({ error: "internal" }); }
});

router.post("/api/pigeons/drag/race", requireTgUser, rateLimit(20), async (req, res) => {
  const u = getTgUser(req)!; const b = req.body as any;
  try {
    const { runRace } = await import("../drag");
    const r = await runRace(u.id, String(b.breed || ""), b.mode === "bet" ? "bet" : "training", Number(b.stake) || 0, Number(b.reactionMs) || 3000);
    if (!r.ok) { res.status(400).json({ error: r.reason }); return; }
    res.json(r);
  } catch (e) { log.error({ err: e, chatId: u.id }, "[drag/race]"); res.status(500).json({ error: "internal" }); }
});
```
Для `/opponents` targetPower: прочитать мой `tune_speed/stamina/stars` из pigeon_inventory (как getTuning) и посчитать `dragPower`. Реализатор берёт готовый паттерн из `getTuning`.

- [ ] **Step 2: Проверить + Commit** — `npx tsc --noEmit` чисто. `git commit -m "feat(drag): HTTP-роуты /api/pigeons/drag/opponents|race"`

---

### Task 5: Клиент — экран драга и canvas-заезд (`public/js/catdrag.js`)

**Files:**
- Create: `public/js/catdrag.js`
- Modify: `public/js/catdove.js` (кнопка входа в секции гонки + мост), `public/game.html`, `public/index.html`

**Interfaces:**
- Consumes: POST `/api/pigeons/drag/opponents`, POST `/api/pigeons/drag/race`; `window.ckApi` (fetch-хелпер), `window.ckFlash` (тосты), мост обновления энергии/баланса кликера.
- Produces: `window.CatDrag = { open(api, breed) }` — открывает оверлей драга для выбранной породы.

- [ ] **Step 1: Кнопка входа в `catdove.js`** — в `raceHtml()` (секция «Гонка стаи») добавить кнопку **«🏁 Драг-заезд»** рядом с недельной заявкой; onclick → выбор голубя (owned) → `window.CatDrag.open(apiRef, breed)`. Bump catdove ?v.

- [ ] **Step 2: `catdrag.js` — оверлей и выбор** — window.CatDrag.open(api, breed): полноэкранный оверлей (свой cd-drag-* CSS namespace, бренд-переменные из .ck-ov). Экран настройки: мой голубь (арт+мощность), режимы Тренировка/Ставка (сегмент), для ставки — выбор суммы из пресетов; превью соперников через `/opponents` (3 карточки арт+мощность). Кнопка «Старт».

- [ ] **Step 3: Заезд (canvas)** — при «Старт»: отсчёт 3-2-1-GO (крупно), замер `t0=performance.now()` на GO; тап по экрану → `reactionMs = now - t0`; POST `/race {breed, mode, stake, reactionMs}`. По ответу — анимировать canvas: 4 дорожки, спрайты = `/img/pigeons/{breed}.webp` (загрузить как Image), позиция каждого = интерполяция к финишу за его `finishT` (нормировать по max finishT в кадры, requestAnimationFrame), камера скроллит фон за лидером, лента финиша. По завершении анимации — плашка: «N место», для ставки — `+/−reward` монет. Обновить энергию/баланс кликера из `newEnergy/newBalance` (мост `window.ck*` — сверить, как catdove обновляет баланс, если моста нет — добавить `window.ckSyncState({balance,energy})` в catclick.js).
- Reason-коды в тосты: `no_energy`→«Не хватает энергии — подожди восстановления», `not_enough_coins`→«Не хватает монет на ставку», `not_owned`→«Птица не найдена».
- Античит: `reactionMs` меряется честно; сервер всё равно зажимает и решает — клиент не доверенный.

- [ ] **Step 4: Подключение** — `<script src="/js/catdrag.js?v=1" defer>` в game.html (+preload) и index.html, рядом с catdove. Bump catdove (изменился) и catclick (если добавлял мост).

- [ ] **Step 5: Проверить + Commit** — `node --check public/js/catdrag.js && node --check public/js/catdove.js && node --check public/js/catclick.js`; `npx tsc --noEmit` (сервер цел). `git commit -m "feat(drag): клиент — экран драга + canvas-заезд, вход из секции гонки"`

---

### Task 6: E2E на VPS + финальное ревью + деплой

**Files:**
- Create: `scripts/e2e/drag-e2e.mjs`

- [ ] **Step 1: E2E-скрипт** (по образцу `scripts/e2e/race-e2e.mjs`) — тестовые chat_id 1.99e12, автоочистка в finally. Проверки:
  - `pickOpponents` возвращает 3 (реальные + добивка ботами при нехватке).
  - `runRace` training: списывает `DRAG_ENERGY_COST` энергии, баланс не тронут, `myPlace ∈ 1..4`.
  - `runRace` bet 1-е место (подстроить сильный голубь vs слабые боты): `+stake`; проигрыш: `−stake`; без энергии → `no_energy`; без монет → `not_enough_coins`.
  - Античит: `reactionMs=0` → в БД `race_reaction_ms=120`.
  Сид: тестовому аккаунту дать голубя + энергию (`UPDATE clicker_state SET energy=1000, balance=... `) + тюнинг.

- [ ] **Step 2: Локальная проверка** — `npx tsc --noEmit && npx vitest run && node --check public/js/catdrag.js` зелёные.

- [ ] **Step 3: Финальное ревью ветки** — dispatch code-reviewer (superpowers:requesting-code-review) на весь диапазон; фиксы Critical/Important.

- [ ] **Step 4: Мерж + push + деплой** — мерж в master, push; деплой на VPS (ssh за юзером через `!` при блокировке классификатором): `cd /opt/maria && git -C maria-bot pull && docker compose up -d --build maria-bot`. Прогнать E2E в контейнере (`docker cp`+`node`), смоук прода (`scripts/smoke.sh`), ручной прогон заезда в браузере.

- [ ] **Step 5: Commit E2E** — `git add scripts/e2e/drag-e2e.mjs && git commit -m "test(drag): E2E заезда против живой БД"`

---

## Отложено (вне плана, YAGNI)

Реалтайм-PvP, запись тап-по-тапу, реальные деньги, апгрейд/кастомизация трасс, турнирные сетки, звук. Только два режима + матч по мощности + canvas-визуал.
